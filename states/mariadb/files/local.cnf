[mysqld]

innodb_buffer_pool_size = {{ (grains['mem_total'] * 0.375) | round | int }}M
innodb_log_file_size = {{ (grains['mem_total'] / 16) | round | int }}M
max_connections = {{ (grains['mem_total'] / 102.4) | round | int }}

performance_schema = ON

query_cache_type = OFF
query_cache_size = 0
